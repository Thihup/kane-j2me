package javax.microedition.io;

import java.io.IOException;

// J2ME Generic Connection Framework (GCF) Stub
public interface Connection extends java.io.Closeable {
    // This is the base interface for all connections.
    // It just adds a close() method, which is already covered by AutoCloseable.
}
